# 构建产物
dist/
wheels/
build/

# Local configuration
router-config.json
.last_config_path
.env
.env.*

# 工具缓存
.dbg/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Local data
data/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# 例外：测试语料里的 .sqlite3 是**随包发布的对拍夹具**，必须入库。
#
# 上面的 *.sqlite3 是为了挡住本地运行产生的指标库（含真实请求记录，属于用户数据）。
# 但 internal/*/testdata 下的那几份是 Python 参照实现写出的固定夹具——Go 测试要靠它们
# 验证「用户换二进制后能直接打开旧库」这条路径。第 39 行会把它们一并忽略掉，导致
# 测试在 CI 上因找不到夹具而失败。用否定规则放行 testdata，注意必须写在忽略规则**之后**。
!internal/**/testdata/*.sqlite3

# Go 构建产物。
#
# `go build ./cmd/amkr` 默认把二进制落在**当前目录**（仓库根），Windows 上是 18 MB 的
# amkr.exe。它绝不该进版本库，而仓库里原本没有任何规则挡住它——`git add .` 这类宽泛暂存
# 会直接把它带上。显式忽略两个名字，避免依赖每个人记得只暂存指定路径。
/amkr
/amkr.exe
# 根目录下的其它构建产物：调试时 go build -o xxx.exe 常落在仓库根，git add -A 会顺手带上。# 用通配覆盖，而不是逐个补名字——名字是临时起的，补不完。
/*.exe